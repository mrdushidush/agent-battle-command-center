import { describe, it } from '@jest/globals';

describe.skip('CodeReviewService', () => {
  it.skip('TODO: Update tests to match current API', () => {
    // Service no longer has getInstance()
    // API changed from shouldReview() to getReviewDecision()
    // No updateConfig() method exists
  });
});
